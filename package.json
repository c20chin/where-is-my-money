{
  "name": "where-is-my-money",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:watch": "cd apps/web && nodemon --config nodemon.json",
    "watch": "turbo watch dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:generate": "turbo db:generate",
    "db:migrate": "turbo db:migrate",
    "db:seed": "turbo db:seed"
  },
  "packageManager": "pnpm@9.15.2",
  "devDependencies": {
    "turbo": "^2.9.6"
  }
}
